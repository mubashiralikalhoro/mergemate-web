export const toAuthPage = {
  redirect: {
    destination: "/auth/login",
    permanent: false,
  },
};

export const toProfilePage = {
  redirect: {
    destination: "/user/profile",
    permanent: false,
  },
};
